{
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "start": "turbo run start",
    "clean": "turbo run clean",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "format": "prettier --write \"**/*.{ts,tsx,md}\""
  },
  "devDependencies": {
    "@rollup/plugin-json": "^6.0.1",
    "eslint": "^8.48.0",
    "prettier": "^3.0.3",
    "tsconfig": "*",
    "turbo": "^2.1.3"
  },
  "name": "trackpackage",
  "description": "TrackPackage shipping platform",
  "author": "TrackPackage",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "packageManager": "npm@10.8.2",
  "dependencies": {
    "next-auth": "^5.0.0-beta.20"
  }
}
